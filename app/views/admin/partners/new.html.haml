.module.form-module.max-width
  .toggle
    %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
      %span{"aria-hidden" => "true"} ×
  .form
    .result
    %h2 Tạo đối tác mới
    = render 'form'

  .cta
    = link_to 'Trở về', admin_partners_path